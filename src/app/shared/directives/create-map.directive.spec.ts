import { CreateMapDirective } from './create-map.directive';

describe('CreateMapDirective', () => {
  it('should create an instance', () => {
    const directive = new CreateMapDirective();
    expect(directive).toBeTruthy();
  });
});
