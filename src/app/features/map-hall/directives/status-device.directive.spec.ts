import { StatusDeviceDirective } from './status-device.directive';

describe('StatusDeviceDirective', () => {
  it('should create an instance', () => {
    const directive = new StatusDeviceDirective();
    expect(directive).toBeTruthy();
  });
});
